# Production Environment Variables Template
# Copy this file to .env.prod and update with your production values
# NEVER commit .env.prod to version control!

# NextAuth Configuration
AUTH_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING_FOR_PRODUCTION

# Google OAuth
GOOGLE_CLIENT_ID=your-production-google-client-id
GOOGLE_CLIENT_SECRET=your-production-google-client-secret

# Microsoft Azure AD OAuth (if using)
AZURE_AD_CLIENT_ID=your-production-azure-client-id
AZURE_AD_CLIENT_SECRET=your-production-azure-client-secret
AZURE_AD_TENANT_ID=your-production-azure-tenant-id

# Backend API Configuration (for frontend container)
NEXT_PUBLIC_API_URL=http://backend:8080

# Database Configuration
POSTGRES_DB=gs_production
POSTGRES_USER=gs_user
POSTGRES_PASSWORD=CHANGE_THIS_TO_A_STRONG_PASSWORD

# IMPORTANT SECURITY NOTES:
# 1. Use strong, unique passwords for all services
# 2. Never use default credentials in production
# 3. Keep this file secure and never commit it to git
# 4. Consider using secrets management (AWS Secrets Manager, HashiCorp Vault, etc.)
# 5. Rotate credentials regularly
